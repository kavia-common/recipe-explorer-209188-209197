This project optionally uses PUBLIC_API_BASE for fetching recipes. If unset, the app falls back to public/data/recipes.json.

ESLint ignores Astro-generated types under .astro/. If you reconfigure lint, ensure generated files are excluded to prevent false positives.
